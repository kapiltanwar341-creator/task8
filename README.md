# task8

# Task 8 — Create a Simple Blog Layout Using Bootstrap 5

A polished, responsive portfolio / projects page built with **Bootstrap 5** and **Bootstrap Icons**. Features include a sticky navbar, animated hero section, interactive project cards with modal details, a theme (dark) toggle, and a modern footer.

---

## Files

```
creative-studio/
├── index.html       # main page (Bootstrap CDN + custom markup)
├── style.css        # custom styling (animations, dark-mode, cards)
├── script.js        # modal handling, theme toggle, project data
└── assets/          # optional local images (if used)
```
